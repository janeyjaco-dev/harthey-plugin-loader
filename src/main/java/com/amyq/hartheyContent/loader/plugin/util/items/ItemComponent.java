package com.amyq.hartheyContent.loader.plugin.util.items;

/**
 * Base interface for all custom item components.
 * 
 * Components are attached through Item.Settings.component(...)
 * and stored inside the Settings.Components collection.
 *
 * This mirrors Fabric's component system: components are simple
 * data holders that can be queried by type.
 */
public interface ItemComponent {
}